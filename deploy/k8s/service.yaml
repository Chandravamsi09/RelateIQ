apiVersion: v1
kind: Service
metadata:
  name: relateiq-backend-service
  namespace: relateiq-prod
  labels:
    app.kubernetes.io/name: relateiq-backend
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP
  selector:
    app.kubernetes.io/name: relateiq-backend
