version: '3.8'

services:
  crm-server:
    build: .
    container_name: relateiq-crm
    ports:
      - "3000:3000"
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - PORT=5000
    restart: unless-stopped
